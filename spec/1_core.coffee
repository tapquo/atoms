describe "Core", ->

  it "is healthy", ->
    expect(Atoms).toBeTruthy()

  it "Atom exists", ->
    expect(Atoms.Atom).toBeTruthy()

  it "Molecule exists", ->
    expect(Atoms.Atom).toBeTruthy()

  it "Organism exists", ->
    expect(Atoms.Atom).toBeTruthy()
